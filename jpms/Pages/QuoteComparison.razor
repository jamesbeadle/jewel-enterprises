@page "/projects/{ProjectId}/procurement/{PackageId}/compare"
@inject AuthService Auth
@inject SessionService Session
@inject IProcurementStore ProcurementStore
@inject NavigationManager Nav

<PageTitle>Quote comparison · JPMS</PageTitle>

@if (!isLoaded)
{
    <section class="max-w-5xl mx-auto px-4 py-12 text-slate-500 text-sm">Loading…</section>
}
else if (!Session.IsApproved)
{
    <RequestAccessView Email="@Auth.CurrentUser!.Email" DisplayName="@Auth.CurrentUser.DisplayName" />
}
else
{
    <ProjectPageShell ProjectId="@ProjectId" ActiveTab="procurement">
        <nav class="mb-4 text-sm">
            <a href="@($"/projects/{ProjectId}/procurement")" class="text-slate-500 hover:text-slate-900 underline underline-offset-4">Procurement</a>
            <span class="text-slate-400 mx-2">/</span>
            <span class="text-slate-700 font-medium">@(package?.Title ?? "Package")</span>
            <span class="text-slate-400 mx-2">/</span>
            <span class="text-slate-700 font-medium">Compare quotes</span>
        </nav>

        @if (package is null)
        {
            <p class="text-sm text-slate-500 italic">Bid package not found.</p>
        }
        else
        {
            <h2 class="text-lg font-semibold text-slate-900 mb-3">@package.Title</h2>
            <QuoteComparisonGrid Quotes="Quotes" />
        }
    </ProjectPageShell>
}

@code {
    [Parameter] public string ProjectId { get; set; } = "";
    [Parameter] public string PackageId { get; set; } = "";

    private bool isLoaded;
    private BidPackage? package;

    private IReadOnlyList<Quote> Quotes => ProcurementStore.QuotesFor(PackageId);

    protected override async Task OnInitializedAsync()
    {
        await Session.EnsureLoadedAsync();
        if (!Auth.IsSignedIn) { Nav.NavigateTo("/login", forceLoad: true); return; }
        package = ProcurementStore.FindPackage(PackageId);
        isLoaded = true;
    }
}
