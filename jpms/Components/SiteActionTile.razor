<a href="@Href"
   class="block rounded-2xl border border-slate-200 bg-white p-5 active:bg-slate-100 transition shadow-sm">
    <div class="flex items-center gap-3">
        <span class="inline-flex items-center justify-center w-12 h-12 rounded-xl bg-slate-900 text-white text-2xl shrink-0"
              aria-hidden="true">
            @Glyph
        </span>
        <div class="min-w-0">
            <p class="text-base font-semibold text-slate-900">@Label</p>
            @if (!string.IsNullOrEmpty(Detail))
            {
                <p class="text-xs text-slate-500 mt-0.5">@Detail</p>
            }
        </div>
    </div>
</a>

@code {
    [Parameter, EditorRequired] public string Href { get; set; } = "";
    [Parameter, EditorRequired] public string Label { get; set; } = "";
    [Parameter, EditorRequired] public string Glyph { get; set; } = "";
    [Parameter] public string Detail { get; set; } = "";
}
