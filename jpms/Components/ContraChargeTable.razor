<div class="rounded-xl border border-slate-200 bg-white overflow-x-auto">
    <table class="w-full text-sm">
        <thead class="bg-slate-50 border-b border-slate-200">
            <tr class="text-left text-[11px] uppercase tracking-wider text-slate-500 font-semibold">
                <th class="px-4 py-3">Subcontractor</th>
                <th class="px-4 py-3">Category</th>
                <th class="px-4 py-3">Status</th>
                <th class="px-4 py-3 text-right">Amount</th>
                <th class="px-4 py-3 text-right">Recovered</th>
            </tr>
        </thead>
        <tbody class="divide-y divide-slate-200">
            @foreach (var contraCharge in ContraCharges)
            {
                <tr>
                    <td class="px-4 py-3 text-slate-900 font-medium">@contraCharge.SubcontractorReference</td>
                    <td class="px-4 py-3 text-slate-700">@contraCharge.Category</td>
                    <td class="px-4 py-3 text-slate-700">@contraCharge.Status</td>
                    <td class="px-4 py-3 text-right text-slate-700">@Money(contraCharge.Amount)</td>
                    <td class="px-4 py-3 text-right text-slate-900 font-medium">@Money(contraCharge.RecoveredAmount)</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    [Parameter, EditorRequired] public IReadOnlyList<ContraCharge> ContraCharges { get; set; } = Array.Empty<ContraCharge>();

    private static string Money(decimal value) =>
        value.ToString("C0", System.Globalization.CultureInfo.GetCultureInfo("en-GB"));
}
