@using Jewel.JPMS.Components.Icons

<button type="button"
        @onclick="OnClick"
        class="w-full inline-flex items-center justify-center gap-3 rounded-xl border border-line bg-surface-raised px-4 py-3 text-content font-medium hover:border-line-strong transition focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-canvas">
    <span class="w-5 h-5 shrink-0 inline-flex items-center justify-center">
        @if (Provider == AuthProvider.Microsoft)
        {
            <MicrosoftLogoIcon />
        }
        else
        {
            <GoogleLogoIcon />
        }
    </span>
    <span>Continue with @ProviderName</span>
</button>

@code {
    [Parameter, EditorRequired] public AuthProvider Provider { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }

    private string ProviderName => Provider switch
    {
        AuthProvider.Microsoft => "Microsoft",
        AuthProvider.Google    => "Google",
        _ => Provider.ToString()
    };
}
