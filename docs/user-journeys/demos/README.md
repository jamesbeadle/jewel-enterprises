# Journey Demos

Static HTML mockups for each journey step, linked from journey Markdown files. These are throwaway scaffolding to support on-site role-play sessions — not production code.

Naming: `NN-{step-slug}.html`, matching the parent journey number.

Once the Blazor Journey Index prototype is scaffolded under `/prototypes/`, these HTML demos can be migrated or replaced.
