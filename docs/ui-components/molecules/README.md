# Molecules

Small groups of atoms working together to do one job.

Examples: search bar (input + button), form field (label + input + help text + error), card header (avatar + title + meta), notification toast.
